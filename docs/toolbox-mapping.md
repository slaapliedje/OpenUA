# Mac Toolbox → Atari mapping

Tracks how each Mac Toolbox manager used by FRUA is satisfied on the Atari
side. The `compat/` shim implements the Mac-facing API; the right-hand column
is what it is built on.

Status: `planned` · `wip` · `done` · `native` (migrated off the shim).

| Mac Toolbox manager | FRUA use                         | Atari implementation                                        | Status  |
|---------------------|----------------------------------|-------------------------------------------------------------|---------|
| QuickDraw           | All drawing, `CopyBits`, regions | Geometry core, the `GrafPort` / `CGrafPort` and Color QuickDraw types, `NewPixMap`, rectangular regions, `GetPort`/`SetPort`, the screen port over the display HAL back buffer (`qd_attach_screen`), the rect primitives (`EraseRect`, `PaintRect`, `FrameRect`), the line family (`MoveTo`, `LineTo`, `GetPen`, `PenSize`, `PenMode`, `PenPat`), the ovals (`PaintOval`, `FrameOval`), `CopyBits` (8-bit same-size `srcCopy`), `ClipRect`, the colour entries (`RGBForeColor`/`RGBBackColor` against a cached CLUT via `qd_set_palette`), and the text family (`TextFont`/`TextSize`/`TextFace`/`TextMode` state, `DrawChar`/`DrawString`/`CharWidth`/`StringWidth` over the sparse 8x8 fallback font in `compat/font_8x8.c`) — every primitive intersects `portRect ∩ visRgn ∩ clipRgn`, pen-using primitives honour `pnSize`, the pen pat-modes (`patCopy`/`patOr`/`patXor`/`patBic`) combine `fgColor` (and at `patCopy`, `bkColor`) with the destination pixel, the 8x8 pen pattern (`pnPat`) gates each pen pixel, the colour entries resolve to the nearest sum-of-squared-difference index, and text honours `srcCopy` / `srcOr` through the glyph bits, in `compat/quickdraw.c`; fill patterns, real NFNT fonts, scaling, and the remaining source transfer modes still to come | wip |
| Color/Palette Mgr   | 256-colour CLUT animation        | Shim caches the CLUT via `qd_set_palette` (forwarding to HAL `set_palette` → XBIOS `VsetRGB` Falcon / `EsetPalette` TT); Mac Palette Manager (`NewPalette`, `SetPalette`, ...) to follow | wip |
| Offscreen GWorlds   | Sprite/buffer composition        | 8-bit paletted offscreen surfaces in the shim               | planned |
| Resource Manager    | `GetResource`, resource fork     | FRSC-archive reader in `compat/resources.c` — `GetResource` / `GetIndResource` / `CountResources` / `SizeResource` / `ReleaseResource` / `ResError` over the `(type,id)` archive, plus the resource-file machinery (`OpenResFile` / `OpenRFPerm` / `UseResFile` / `CurResFile` / `CloseResFile` / `HomeResFile` / `CreateResFile`) over a 4-slot refnum table whose entries all alias the single in-memory archive. Real multi-archive support, file-backed `OpenResFile`, resource attributes, named lookups, and writing resources still to come; `rsrcpack` (the packer) still to build. | wip |
| Memory Manager      | `NewHandle`/`NewPtr`, `HLock`    | `NewPtr` / `NewHandle` over the C heap (`compat/macmemory.c`); handles get a stable master pointer (heap never relocates); plus `BlockMove` / `BlockMoveData` (memmove), `PtrToHand` / `HandToHand` / `HandAndHand` / `PtrAndHand` cross-allocation helpers. `Munger` and pointer-block resizing still to come | done |
| File Manager        | `FSOpen`/`FSRead`, HFS paths     | High-level data-fork API over GEMDOS in `compat/files.c` — `FSOpen` / `FSClose` / `FSRead` / `FSWrite` / `GetEOF` / `SetEOF` / `GetFPos` / `SetFPos` / `Create` / `FSDelete` / `GetFInfo` / `SetFInfo` / `GetVol` / `SetVol` / `FlushVol`. The GEMDOS file handle doubles as the Mac refNum; Mac Pascal paths collapse to their trailing filename; FInfo (type / creator / flags) round-trips through a small in-shim cache because GEMDOS has no Finder bytes. Resource-fork I/O (`OpenRF` / `CreateResFile`) stays in `compat/resources.c` — served by the flat `(type,id)` archive (ADR-0007). | wip |
| Sound Manager       | `SndPlay`, sound channels        | Mac surface in `compat/sound.c`: `SndChannel` / `SndCommand` at the Mac record offsets, `SndNewChannel` / `SndDisposeChannel` / `SndPlay` / `SndDoCommand` / `SndDoImmediate` / `SndSoundManagerVersion`. `SndPlay` parses the Mac `snd ` format-1 resource, locates the SoundHeader (stdSH only), converts the unsigned-8-bit samples to Falcon signed-8-bit, and hands them to the platform HAL. Falcon DMA backend in `platform/sound_falcon.c` — `Locksnd` / `Setbuffer` / `Setmode(MODE_MONO)` / `Settracks(0,0)` / `Devconnect(DMAPLAY, DAC, CLK25M, CLKxx, 1)` / `Buffoper(SB_PLA_ENA)`. Sample-rate match picks the closest of the eight Falcon CODEC rates (Mac 22 kHz → CLK20K @ 19668 Hz). Stereo, 16-bit, looping, extSH / cmpSH compression, and the YM2149 fallback on TT030 follow when an engine path demands them. | wip |
| Event Manager       | `GetNextEvent`, mouse/keyboard   | `EventRecord`, the masks / modifiers, a 16-slot posted FIFO, `TickCount` over `_hz_200`, `GetNextEvent` / `EventAvail` / `PostEvent` / `FlushEvents` / `WaitNextEvent`, keyboard via BIOS `Bconstat` / `Bconin` + `Kbshift`, mouse position / button via the IKBD-packet handler (a Supexec-installed `mousevec` trampoline in `platform/input.c` decoding the 3-byte packets into `g_mouse_x` / `g_mouse_y` / `g_mouse_btn`), `mouseDown` / `mouseUp` synthesised from button-state edges on each `GetNextEvent` pump (re-posted into the queue when the caller's mask drops them, so up/down pairs never get lost), and updateEvt synthesis from any window's non-empty `updateRgn` (`compat/events.c`); `activateEvt` and `autoKey` follow | wip |
| Cursor Manager      | Arrow + watch (`InitCursor`/`SetCursor`/`GetCursor(4)`) | Software cursor in `compat/quickdraw.c`: one current `Cursor` (16x16 image+mask+hotspot) + a visibility level; `InitCursor` / `SetCursor` / `ShowCursor` / `HideCursor` / `ObscureCursor` / `GetCursor` (generic arrow + watch shapes, not FRUA art). The present path (`qd_present`) composites the cursor onto the back buffer at the live IKBD mouse position — `data` bit → nearest-black, `mask` bit → nearest-white via `qd_nearest_color` — saving and restoring the pixels underneath so the engine's buffer is untouched between frames (no trails). Installed at boot (`main.c`); the title sequence presents each spin so it tracks. Colour `crsr` / `SetCCursor` and per-`present_rect` compositing follow. | wip |
| Window Manager      | Editor & play-UI windows         | Reimplemented in the shim — window record, list, lifecycle, geometry, the update mechanism, b&w / colour / resource-loaded windows, structure / content regions in global screen coords, the title in a Handle, the desktop-side frame drawing (1-pixel black outline, grey title bar with the title centred, close box on left) painted into the screen port on `ShowWindow` / `SelectWindow`, and the user-action plumbing (`FindWindow` over the window stack, `DragWindow` with an XOR outline + `MoveWindow` on release, `TrackGoAway` against the close box) — `DragWindow` / `TrackGoAway` spin on `Button()` / `GetMouse()` so they go live the moment the IKBD-packet driver promotes the stubs, in `compat/windows.c`; active/inactive title-bar styling and `GrowWindow` / `SizeWindow`'s grow box follow | wip |
| Menu / Dialog       | Editor & tools UI                | Reimplemented in the shim; Mac-style widgets drawn into the HAL surface (ADR-0006). Dialog Manager: `Alert(id, filter)` parses ALRT + DITL from the resource fork, opens a modal window, paints button frames + static text into the screen port, spins a small event loop until keypress / click dismisses. `GetNewDialog` / `NewDialog` load a DLOG resource (and follow its `itemsID` into the DITL), open a colour window in the DLOG bounds with the parsed title, and return a `DialogRecord` (WindowRecord + items Handle + aDefItem). `DrawDialog` paints the DITL items into the screen port. `ModalDialog(filter, &itemHit)` runs a `WaitNextEvent` loop and dispatches each event through `DialogSelect`, which returns the 1-based item of an enabled button under a `mouseDown` (or `aDefItem` on Return / Enter). `DisposeDialog` tears down. Edit-text items (DITL type 0x10) are live: NewDialog scans the DITL for editText items and allocates per-item Pascal-string buffers seeded from the DITL data bytes; DrawDialog paints each as a white-filled box with a 1-px black border and renders an insertion caret at the end of the focused field; the focus starts on the first editText (or moves via Tab); DialogSelect routes mouseDown over an editText to focus and keyDown to the focused field (backspace removes the last byte, printable ASCII appends). The default item now paints with the Mac's double-ring border. `dialog_get_edit_text` / `dialog_set_edit_text` round-trip the field text by DialogPtr + item index. `GetDialogItem` returns the item type and local rect; `SetDialogItemText` / `GetDialogItemText` (the Mac Handle-based API) are stubs pending per-item StringHandle allocation. Menu Manager skeleton (`compat/menus.c`): the Mac-faithful MenuInfo header (engine by-offset access to menuID / menuWidth / menuHeight / menuProc / enableFlags ports unchanged) with a shim-private item slab; `NewMenu`, `GetMenu` (loads MENU resource), `DisposeMenu`, `AppendMenu` (`;`-separated Pascal-string items, meta-character tail stripped), `CountMItems`, `GetMenuItemText`, `EnableItem` / `DisableItem` flipping bits in enableFlags, `InsertMenu` / `DeleteMenu` / `ClearMenuBar` over a 16-slot menu bar, `GetMenuHandle` / `GetMHandle`, `DrawMenuBar` painting a 14-pixel-tall white strip across the top of the screen port with each menu title rendered through the QuickDraw text family, `HiliteMenu` inverting a single title, `MenuSelect` tracking a pull-down across the bar with Button() / GetMouse() — the bits under the dropdown are stashed in a NewPtr block and blitted back on dismiss so any windows it briefly covered come back untouched — and `MenuKey` for Cmd-key dispatch (scans the bar for an enabled item whose `/`-meta key-equivalent matches the keystroke, flashes the menu title once on hit, returns the same packed `(menuID, item)` as a MenuSelect); FindWindow returns the new `inMenuBar` part code for any click in the bar strip. Styled item text, item icons, MDEF dispatch, and the full `GetNewDialog` / `ModalDialog` / `DialogSelect` machinery still to come. | wip |
| Control Manager     | Buttons, checkboxes, radios, scroll bars | Reimplemented in the shim — `compat/controls.c`. ControlRecord at the Mac 44-byte layout; the per-window linked list hangs off `controlList`. `NewControl` / `GetNewControl` (CNTL resource) / `DisposeControl` / `KillControls` (called by `DisposeWindow`). Visibility / hilite / value / range / title / refCon accessors. `Draw1Control` and `DrawControls` paint the three primary CDEFs directly: pushButProc (white-filled rectangle with centred title, inverted when hilited), checkBoxProc (12-px tick box + label, X drawn when value != 0), radioButProc (12-px circle + label, filled inner dot when value != 0). Disabled (hilite = 255) renders with grey. `TestControl` / `FindControl` hit-test using window-local coords. `TrackControl` spins on Button() / GetMouse() with the Mac's "hilite tracks the mouse in/out of the rect" contract and returns the part code on release-inside. Scroll bars, the CDEF dispatch, control colour, and the Dialog Manager DITL type-4 → ControlHandle integration follow. | wip |
| TextEdit            | Editor text fields               | Skeleton in `compat/textedit.c`. `TERec` at the Mac record offsets (engine by-offset access ports unchanged), `TENew` / `TEDispose`, `TESetText` / `TEGetText` round-trip through the `hText` Handle, `TESetSelect`, `TEKey` (printable ASCII insert, backspace, left / right / up / down arrows with single-line home / end), `TEClick` (caret-by-pixel hit-test against the 8x8 fallback font advance), `TEActivate` / `TEDeactivate`, `TEUpdate` (white background + text via the QuickDraw text family + a vertical caret on the active field), `TEIdle` (caret blink at ~30-tick cadence). Single-line behavior in the v1 skeleton; word wrap, multi-line scroll, justification, click-loop / word-break hooks, cut / copy / paste scrap, the TextEdit 1.2 styled variant, and Dialog Manager DITL editText → `TEHandle` integration all follow. | wip |
| OSUtils / ToolUtils | Misc (time, fixed-point, etc.)   | Case by case; mostly trivial inline equivalents             | planned |

## Toolbox startup

`compat/toolbox.c` provides the seven Mac Toolbox manager init entry points
— `InitGraf`, `InitFonts`, `InitWindows`, `InitMenus`, `TEInit`,
`InitDialogs`, `FlushEvents` — and `toolbox_init()`, which calls them in the
standard Mac startup order. The engine wires this up via `src/engine/master.c`'s
`master_init`, which calls `toolbox_init()` in place of the lifted `JT[1144]`
prologue. `src/main.c` brings up the display HAL and the input HAL, then
hands off to `ua_main` (`src/engine/boot.c`); the engine takes over from
there.

## Decided

- **Resource fork** → flat `(type, id)` archive built by `tools/rsrcpack`,
  served by the Resource Manager shim. See ADR-0007 and `tools/README.md` for
  the archive format.
- **Editor UI** → Menu/Window/Dialog/Control/TextEdit reimplemented inside the
  shim, drawn into the HAL surface. See ADR-0006.
- **Phasing** → the play-UA-modules runtime is ported first; the design tools
  follow as a second phase. See ADR-0008.

## Still open

_None right now._ The Mac release has been unpacked (see
`docs/mac-release.md`); the application resource fork is extracted raw, so
`rsrcpack` consumes a raw resource fork and `tools/appledouble.py` handles
the AppleDouble unwrapping upstream.
