# Build output
*.o
*.a
*.d
*.prg
*.tos
*.app
bin/
build/

# Original copyrighted FRUA assets — never commit these
/data/*
!/data/README.md

# Generated FRSC archive (built from the copyrighted resource fork)
/frua.rsc

# Generated DATA + DREL replay tables. The Makefile regenerates them
# from frua.rsc on every build (stubbed when the fork is absent). The
# real-data form contains 12 KB of copyrighted FRUA application bytes,
# so the files stay untracked.
/src/engine/data_pool.h
/src/engine/data_pool.c

# Decompilation scratch (Ghidra / IDA / Hopper)
*.idb
*.i64
*.hop
ghidra_projects/

# Python host tooling
tools/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Engine test-run output written into the GEMDOS C: mount (the repo dir):
# character saves from the cg-audit / probe self-tests. Never committed.
*.CCH
*.CHR
*.cch
*.chr

# Editor / OS noise
.DS_Store
*.swp
*~
dist/

# Personal Claude Code config (permission grants, local paths) — never public
.claude/settings.local.json
frua
.machine
uainst.ttp
uainst_amiga
build.err
