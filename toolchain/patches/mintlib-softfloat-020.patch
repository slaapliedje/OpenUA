diff --git a/Makefile b/Makefile
index 533bed9..5c8074b 100644
--- a/Makefile
+++ b/Makefile
@@ -42,8 +42,8 @@ ifeq ($(WITH_DEBUG_LIB), yes)
 endif
 
 ifeq ($(WITH_020_LIB), yes)
-  SUBDIRS += lib020
-  DIST_SUBDIRS += lib020
+  SUBDIRS += lib020 lib020sf
+  DIST_SUBDIRS += lib020 lib020sf
 endif
 
 ifeq ($(WITH_V4E_LIB), yes)

# --- new file: lib020sf/Makefile (copy of lib020/Makefile with: ---
#       subdir = lib020sf
#       instdir = m68020-60/msoft-float
#       cflags = -m68020-60 -msoft-float
#     and copy lib020/{BINFILES,SRCFILES,EXTRAFILES,MISCFILES,README} into lib020sf/)
